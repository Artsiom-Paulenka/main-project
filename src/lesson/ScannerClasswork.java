package lesson;

import java.util.Locale;
import java.util.Scanner;

public class ScannerClasswork {
    public static void main(String[] args) {
        //Scanner sc = new Scanner(System.in);
        // int echo = sc.nextInt();
        //String hello = sc.nextLine();
        //String world = sc.nextLine();
        //String answer = hello + world;
        //System.out.println(answer);

        Scanner sc = new Scanner(System.in); //Создание сканера
        String password = sc.nextLine();// считыванеи строки
        //password = "D" + password;//password присвой значение паролю маленькими буквами
        //int size = password.length() - 5;
        //password = password + size;
        //password = password.replace('Z','~');
        //char symbol = password.charAt(6);
        //int length = password.length();
        //char length
        //System.out.println(symbol);


    }
}
